================================================================================
SALARYING - DOCUMENTO TECNICO COMPLETO
Generado:        2026-05-06
Ultima actualizacion: 2026-05-13 (sesion - subcategorias + modulo Gustitos)
Version App:     1.2.0+3
================================================================================

================================================================================
1. INFORMACION GENERAL
================================================================================

Nombre:              Salarying
Descripcion:         Gestor de finanzas personales y pequeños negocios
Version Flutter:     1.2.0+3
Package ID Android:  com.example.salarying
Plataformas:         Android (principal), iOS, Web, Windows, Linux, macOS

URLs de Produccion:
  Backend REST:      https://presupuestos-backend-h3l6.onrender.com
  Base de datos:     bvmmbs7zrnufnskqoxwm-mysql.services.clever-cloud.com
  Firebase Project:  salary-bi (proyecto: 750057422826)

Repositorio:         https://github.com/jcharrisdev/presupuestos-backend

================================================================================
2. STACK TECNOLOGICO
================================================================================

FRONTEND (Flutter/Dart)
  Framework:         Flutter SDK 3.29.3
  Lenguaje:          Dart
  Autenticacion:     google_sign_in ^6.2.1
  HTTP:              http ^1.2.0
  Cache local:       hive ^2.2.3 + hive_flutter ^1.1.0
  Notificaciones:    flutter_local_notifications ^17.2.4
  Generacion PDF:    pdf ^3.10.8 + printing ^5.12.0
  Calendario UI:     table_calendar ^3.1.2
  Fechas:            intl ^0.19.0

BACKEND (Node.js)
  Framework:         Express.js ^4.21.0
  Base de datos:     mysql2 ^3.16.0 (con Promises y Pool)
  CORS:              cors ^2.8.5
  Cron jobs:         node-cron ^3.0.3
  HTTP client:       node-fetch ^3.3.2
  Puerto:            3002 (configurable por env PORT)
  Version servidor:  2.6

INFRAESTRUCTURA
  Backend hosting:   Render (plan gratuito, duerme tras inactividad)
  Base de datos:     Clever Cloud MySQL (max 5 conexiones por usuario)
  Autenticacion:     Firebase / Google Sign-In
  Keystore release:  android/app/release.jks (alias: salarying)

================================================================================
3. AUTENTICACION
================================================================================

Sistema: Google Sign-In (google_sign_in package)
ID de usuario: email de la cuenta Google (usado como firebase_uid en todos los endpoints)

FLUJO DE AUTENTICACION:
  1. App inicia → main() ejecuta AuthService.silentSignIn()
  2. Si hay sesion activa → ir directo a MainMenu (sin pantalla de login)
  3. Si no hay sesion → mostrar LoginScreen
  4. Usuario presiona "Sign in with Google" → abre selector de cuenta
  5. Login exitoso → navegar a MainMenu con email y displayName
  6. En logout: AuthService.signOut() → limpia cache Hive → volver a LoginScreen

IDENTIFICADOR DE USUARIO:
  - firebase_uid = email de Google (ej: usuario@gmail.com)
  - Se pasa como query param en todos los GET y en el body de POST/PUT/DELETE
  - Permite recuperar datos sin migracion si el usuario cambia de dispositivo

ARCHIVOS RELACIONADOS:
  lib/services/auth_service.dart    → wrapper Google Sign-In
  lib/login_screen.dart             → pantalla de login
  lib/main.dart (_AuthGate)         → puerta de autenticacion
  android/app/google-services.json  → configuracion Firebase Android

HUELLAS SHA-1 REGISTRADAS EN FIREBASE:
  Debug:   06:42:B4:E6:CF:30:AE:6D:03:1C:7D:F3:92:62:73:EF:C1:45:D2:F6
  Release: D4:0A:89:75:EF:A9:C4:72:DE:40:04:E4:39:6D:3B:20:5D:2D:C4:59

================================================================================
4. ARQUITECTURA
================================================================================

Flutter App
    │
    │ HTTP (ApiClient, timeout 55s)
    ▼
REST API (Express.js en Render)
    │
    │ mysql2 Pool (max 3 conexiones - limite Clever Cloud)
    ▼
MySQL (Clever Cloud)

COMPONENTES INTERNOS FLUTTER:
  AuthService        → Google Sign-In
  ApiClient          → cliente HTTP centralizado (base URL + timeout)
  CacheService       → Hive (cache local 24h TTL)
  NotifService       → notificaciones locales Android
  PdfService         → generacion de PDF
  SharedBudgetService → CRUD presupuestos compartidos
  ServiciosService   → CRUD modulo Ofrecimiento de Servicios (jobs)
  GustitosService    → CRUD modulo Gustitos (compras espontaneas)

ESTRATEGIA DE CACHE:
  - Lista presupuestos: cache-first (Hive, 24h TTL)
  - Lista ventas: cache-first (Hive, 24h TTL)
  - Detalles/detalle: siempre desde API (sin cache)
  - Al logout: se limpia todo el cache del usuario

TIMEOUT:
  - 55 segundos en ApiClient (Render duerme tras inactividad, tarda ~50s en despertar)

================================================================================
5. ESTRUCTURA DE ARCHIVOS
================================================================================

presupuestos-backend/
├── lib/
│   ├── main.dart
│   ├── login_screen.dart
│   ├── main_menu.dart                      ← ACTUALIZADO: navega a VentasLandingScreen
│   ├── ventas_landing_screen.dart          ← NUEVO: landing con 2 divisiones comerciales
│   ├── dashboard_screen.dart               ← REDISEÑADO: dashboard Harvard completo
│   ├── lista_presupuestos.dart
│   ├── crear_presupuesto.dart
│   ├── editar_presupuesto.dart
│   ├── detalles_presupuesto.dart
│   ├── presupuestos_service.dart
│   ├── ahorro_meta.dart
│   ├── progreso_ahorro.dart
│   ├── calendario.dart
│   ├── cobros_home.dart                    ← SIN CAMBIOS (Venta de productos)
│   ├── productos_screen.dart
│   ├── produccion_detalle.dart
│   ├── venta_detalle.dart
│   ├── lista_compras_screen.dart
│   ├── cliente_estado_cuenta_screen.dart
│   ├── receta_screen.dart
│   ├── shared_budget_detail.dart           ← REDISEÑADO: igual a presupuesto normal
│   ├── shared_budget_create.dart
│   ├── shared_budget_invitations.dart
│   ├── shared_budgets_list_screen.dart
│   ├── servicios/                          ← modulo Ofrecimiento de Servicios
│   │   ├── servicios_dashboard.dart
│   │   ├── crear_trabajo_screen.dart
│   │   ├── trabajo_detalle_screen.dart
│   │   ├── registro_pago_cliente_screen.dart
│   │   ├── registro_gastos_screen.dart
│   │   └── financiero_dashboard_screen.dart
│   ├── gustitos/                           ← NUEVO: modulo Gustitos
│   │   ├── gustitos_screen.dart            ← lista de Gustitos con total y FAB
│   │   └── crear_gustito_sheet.dart        ← bottom sheet crear Gustito
│   ├── theme/
│   │   └── app_theme.dart
│   ├── services/
│   │   ├── auth_service.dart
│   │   ├── api_client.dart
│   │   ├── cache_service.dart
│   │   ├── notification_service.dart
│   │   ├── pdf_service.dart
│   │   ├── shared_budget_service.dart      ← ACTUALIZADO: confirmarMiParte, requestDelete
│   │   ├── servicios_service.dart          ← CRUD jobs/servicios
│   │   ├── gustitos_service.dart           ← NUEVO: CRUD Gustitos
│   │   ├── products_service.dart
│   │   ├── sales_service.dart
│   │   ├── savings_service.dart
│   │   └── calendar_service.dart
│   └── widgets/
│       ├── widgets.dart
│       ├── confirm_dialog.dart
│       ├── empty_state.dart
│       ├── section_header.dart
│       ├── presupuestos/
│       │   ├── balance_card.dart
│       │   ├── pago_form_sheet.dart
│       │   └── gasto_form_sheet.dart       ← ACTUALIZADO: campo subcategoria
│       └── ventas/
│           ├── cliente_tile.dart
│           ├── margen_objetivo_card.dart
│           ├── costos_produccion_card.dart
│           └── reporte_produccion_modal.dart
├── backend/
│   ├── server.js                           ← ACTUALIZADO: +13 rutas (Gustitos) + subcategoria
│   ├── package.json
│   ├── migration_shared_budgets.sql
│   └── .env
├── database/
│   └── migrations/
│       ├── add_gustitos_subcategorias.sql  ← NUEVO: tabla gustitos + col subcategoria
│       ├── add_servicios_jobs.sql
│       ├── add_indexes.sql
│       ├── add_aportaciones_ahorro.sql
│       └── add_historial_precios.sql
├── android/
│   ├── app/
│   │   ├── build.gradle
│   │   ├── google-services.json
│   │   ├── release.jks
│   │   └── src/main/AndroidManifest.xml
│   └── key.properties
├── pubspec.yaml
├── keystore-credentials.enc
└── SALARYING_TECNICO.tct

================================================================================
6. NAVEGACION (ARBOL DE RUTAS)
================================================================================

No usa router central. Navega con Navigator.push() directo.

main()
├── SplashScreen (verificando sesion)
├── LoginScreen (sin sesion)
└── MainMenu (con sesion)
    ├── DashboardScreen                     ← REDISEÑADO completo
    ├── [Card expandible: Presupuesto]      ← NUEVO: submenu con dos opciones
    │   ├── ListaPresupuestos (Individual)
    │   │   ├── CrearPresupuesto (FAB)
    │   │   └── DetallesPresupuesto (tap tarjeta)
    │   │       ├── EditarPresupuesto (boton editar)
    │   │       └── [exportar PDF] (boton PDF)
    │   └── SharedBudgetsListScreen (Compartido)
    │       ├── SharedBudgetCreateScreen (FAB)
    │       ├── SharedBudgetInvitationsScreen (icono buzón)
    │       └── SharedBudgetDetailScreen (tap tarjeta) ← REDISEÑADO
    ├── AhorroMetaScreen
    │   └── ProgresoAhorroScreen (boton "ver progreso")
    ├── CalendarioScreen (3 tabs: Calendario / Lista / Flujo)
    └── VentasLandingScreen                 ← NUEVO: elige entre 2 divisiones
        ├── CobrosHome (3 tabs: Productos / Produccion / Ventas)  [Venta de productos]
        │   ├── [Tab Productos] → ProductosScreen
        │   │   └── RecetaScreen (boton "Receta" en variante)
        │   ├── [Tab Produccion] → ProduccionDetalle (tap tarjeta)
        │   └── [Tab Ventas] → VentaDetalle (tap tarjeta)
        │       ├── ListaComprasScreen (boton lista compras)
        │       └── ClienteEstadoCuentaScreen (boton estado cuenta)
        └── ServiciosDashboard              [Ofrecimiento de servicios] ← NUEVO
            ├── FinancieroDashboardScreen (icono grafico en AppBar)
            └── CrearTrabajoScreen (FAB)
                └── TrabajoDetalleScreen (tap tarjeta / tras crear)
                    ├── RegistroPagoClienteScreen (boton + en pagos cliente)
                    └── RegistroGastosScreen (boton + en gastos operativos)

================================================================================
7. MODULOS Y PANTALLAS
================================================================================

────────────────────────────────────────────────────────
7.1 main.dart
────────────────────────────────────────────────────────
Tipo:       Core / punto de entrada
Funcion:    Inicializa la app, verifica sesion, decide que pantalla mostrar

Componentes:
  routeObserver    → RouteObserver global (FIX bug cobros-calendario)
  main()           → inicializa Hive, NotificationService, CacheService, intl español
  MyApp            → MaterialApp con AppTheme
  _AuthGate        → decide LoginScreen vs MainMenu
  _SplashScreen    → pantalla de carga durante verificacion de sesion

────────────────────────────────────────────────────────
7.2 login_screen.dart
────────────────────────────────────────────────────────
Tipo:       Screen
Funcion:    Pantalla de autenticacion con Google

Elementos:
  - Logo de la app
  - Boton "Sign in with Google" con logo Google (CustomPaint)
  - Manejo de errores con SnackBar
  - Navega a MainMenu tras autenticacion exitosa

Servicios: AuthService.signIn()

────────────────────────────────────────────────────────
7.3 main_menu.dart  [ACTUALIZADO 2026-05-11]
────────────────────────────────────────────────────────
Tipo:       Screen
Funcion:    Menu principal con navegacion a modulos

CAMBIO: El boton "Mis Presupuestos" y el boton "Compartido" fueron fusionados
en un unico widget _PresupuestoExpandCard que se expande al tocarlo.

Elementos:
  - AppBar con avatar del usuario (foto Google o inicial)
  - Card Dashboard
  - _PresupuestoExpandCard (expandible con animacion)
      → Sub-card "Presupuesto Individual" → ListaPresupuestos
      → Sub-card "Presupuesto Compartido" → SharedBudgetsListScreen
  - Card Ahorro y Metas
  - Card Calendario
  - Card Cobros
  - Indicador de estado del sistema
  - Boton logout

Widgets nuevos en este archivo:
  _PresupuestoExpandCard  → StatefulWidget con AnimationController y AnimatedSize
  _SubCard                → tarjeta mas pequena para sub-opciones del menu
  _NavCard                → tarjeta de navegacion principal (sin cambios)

Datos propagados: firebaseUid (email), displayName, photoUrl a todas las pantallas

────────────────────────────────────────────────────────
7.4 dashboard_screen.dart  [REDISEÑADO 2026-05-11]
────────────────────────────────────────────────────────
Tipo:       Screen
Funcion:    Panel financiero de nivel Harvard con 7 cards

CAMBIO CRITICO - CAMPOS CORRECTOS DE LA API:
  Presupuesto:
    - gastado NO viene del endpoint GET /presupuestos
    - Se debe llamar GET /presupuestos/:id/detalle y calcular:
        gastado = SUM(movimiento.monto_pagado_real) WHERE pagado=1
    - fecha_inicio/fecha_fin vienen de detalle.periodo
    - total_fijos/total_variables/total_ahorro vienen de detalle.resumen
      (totalFijo, totalNoFijo, totalAhorro)
  Ahorros:
    - El campo meta se llama 'monto_meta' (NO 'monto')
    - El campo ahorrado se llama 'monto_ahorrado' (NO 'total_ahorrado')
    - El total real = monto_ahorrado + total_aportaciones

Cards del dashboard (en orden):
  1. SALUD FINANCIERA
     - Score 0-100 con gauge circular
     - Color: Excelente (≥90 verde), Buena (≥75 verde), Regular (≥60 amarillo),
              Baja (≥40 naranja), Critica (<40 rojo)
     - Insights automaticos: ritmo de gasto, tasa ahorro, deudas compartidas,
       pagos urgentes
     - Formula del score:
         Control presupuesto  → 40 pts (segun diferencia gasto% vs periodo%)
         Tasa de ahorro       → 30 pts (≥20%=30, ≥10%=20, ≥5%=10)
         Balance compartido   → 20 pts (sin deuda=20, <$50=15, <$200=8)
         Flujo positivo       → 10 pts (cobrado >= gastado)

  2. PERIODO ACTUAL
     - Nombre del presupuesto activo
     - Monto grande (gastado) vs monto chico (presupuesto total)
     - Barra de gasto (amarilla → roja al 90%)
     - Barra del periodo transcurrido (gris)
     - Indicador de ritmo: "Ritmo alto/ideal/bajo"

  3. FLUJO NETO + TASA AHORRO (dos mini cards lado a lado)
     - Flujo neto = total_cobrado (ventas) − gastado real
     - Tasa ahorro = total_ahorro del presupuesto / monto_total × 100%

  4. AHORRO Y METAS
     - Meta con mayor progreso porcentual
     - Barra de progreso coloreada
     - Cantidad de metas restantes

  5. DEUDAS COMPARTIDAS
     - Balance neto de todos los presupuestos compartidos activos
     - Desglose por presupuesto (max 3)

  6. PROXIMOS 7 DIAS  [NUEVO: dividido en dos columnas]
     - Columna izquierda "Gastos" con puntos ROJOS (tipo != 'cobro')
     - Columna derecha "Ingresos" con puntos VERDES (tipo == 'cobro')
     - Cada item: nombre, monto, dias restantes (Hoy/Mañana/N dias)

  7. COBROS ACTIVOS
     - Total de ventas activas y monto cobrado

APIs que consume:
  GET /presupuestos?firebase_uid=xxx               → lista para obtener ID activo
  GET /presupuestos/:id/detalle?firebase_uid=xxx   → gastado real, fechas, breakdown
  GET /ahorros?firebase_uid=xxx                    → metas de ahorro
  GET /ventas?firebase_uid=xxx                     → ventas con total_cobrado
  GET /calendario/eventos?firebase_uid=xxx&mes=M&anio=A → proximos 7 dias
  SharedBudgetService.getAll(uid)                  → balance compartido

Caracteristicas:
  - Carga en paralelo (5 endpoints)
  - Pull-to-refresh
  - Cada card es clickeable y navega al modulo correspondiente

────────────────────────────────────────────────────────
7.5 lista_presupuestos.dart
────────────────────────────────────────────────────────
Tipo:       Screen
Funcion:    Listado de presupuestos individuales del usuario

Datos: id, nombre, monto_total, tipo_periodo, dia_inicio_periodo

APIs:
  GET /presupuestos?firebase_uid=xxx

Caracteristicas:
  - Cache-first (Hive, 24h TTL)
  - Pull-to-refresh manual
  - Tarjetas con badge de tipo (Quincenal / Mensual)
  - FAB para crear nuevo presupuesto → CrearPresupuesto
  - Tap tarjeta → DetallesPresupuesto

────────────────────────────────────────────────────────
7.6 detalles_presupuesto.dart  [PANTALLA MAS COMPLEJA]  [ACTUALIZADO 2026-05-13]
────────────────────────────────────────────────────────
Tipo:       Screen
Funcion:    Detalle completo del periodo activo de un presupuesto

Tabs:
  Tab 1 → Periodo actual (movimientos, balance, pagos, Gustitos)
  Tab 2 → Historial (ultimos 6 periodos, lazy loading)

APIs:
  GET /presupuestos/:id/detalle?firebase_uid=xxx
  GET /presupuestos/:id/historico?firebase_uid=xxx
  POST /presupuestos/:id/movimientos
  PUT /movimientos/:id/pagar
  PUT /presupuestos/:id/gastos/reanudar-fijos
  GET /presupuestos/:id/gustitos/summary?firebase_uid=xxx (indirecto via _cargar)
  POST /gustitos (via CrearGustitoSheet)

Respuesta del detalle (ACTUALIZADA):
  {
    periodo: { id, fecha_inicio, fecha_fin, estado },
    movimientos: [{ id, descripcion, monto, tipo, pagado, monto_pagado_real,
                    subcategoria }],   ← NUEVO
    resumen: { totalFijo, totalNoFijo, totalAhorro, totalGastado,
               totalGustitos,          ← NUEVO
               balance_disponible, porcentajePagados }
  }
  NOTA: totalGastado en resumen usa m.monto (presupuestado), NO monto_pagado_real.
        Para gastado real usar SUM(monto_pagado_real) WHERE pagado=1.
  NOTA: balance_disponible = monto_total - totalGastado - totalGustitos

CAMBIOS 2026-05-13:
  - Estado: totalGustitos (double) leido de resumen.totalGustitos
  - Build: totalConGustitos = totalGastado + totalGustitos para BalanceCard y pctGasto
  - Seccion GUSTITOS entre los botones de accion y la seccion MOVIMIENTOS:
      Card con total, boton + (abre CrearGustitoSheet) y boton → (abre GustitosScreen)
  - Stat card extra "Gustitos" aparece si totalGustitos > 0
  - MovimientoTile: muestra subcategoria como chip pequeño gris si existe
  - _agregarGasto() acepta subcategoria opcional y la pasa al body de POST /gastos
    y al item de POST /presupuestos/:id/movimientos
  - _modalNuevoGasto() pasa subcategoria del GastoFormSheet a _agregarGasto()

────────────────────────────────────────────────────────
7.7 shared_budgets_list_screen.dart
────────────────────────────────────────────────────────
Tipo:       Screen
Funcion:    Lista de presupuestos compartidos del usuario

Datos por tarjeta:
  - nombre, estado (active/waiting_for_members/paused/closed)
  - regla_reparto (equitativo/porcentual/proporcional)
  - balance_neto (debes/te deben/sin deudas)

APIs:
  GET /shared-budgets?firebase_uid=xxx
  GET /shared-budget-invitations?firebase_uid=xxx   → badge de invitaciones pendientes

Acciones:
  - FAB "Nuevo" → SharedBudgetCreateScreen
  - Icono buzón → SharedBudgetInvitationsScreen
  - Tap tarjeta → SharedBudgetDetailScreen

────────────────────────────────────────────────────────
7.8 shared_budget_detail.dart  [REDISEÑADO 2026-05-11]
────────────────────────────────────────────────────────
Tipo:       Screen
Funcion:    Detalle de presupuesto compartido, igual en estructura al individual

CAMBIO: Rediseño completo. Antes tenia 2 tabs (Gastos/Balance).
        Ahora es un scroll único igual al presupuesto normal.

Estructura de la pantalla:
  AppBar → nombre del presupuesto + botones (refresh, eliminar)
  ↓
  Banner de periodo con regla de reparto
  ↓
  Card resumen "Mi presupuesto total"
    - Numero grande: mi total de responsabilidades
    - Barra de progreso (pagado / total)
    - "Pagado $X  Pendiente $Y"
  ↓
  Card progreso de pagos circular
    - "X de Y movimientos confirmados"
  ↓
  Botones: [+ Agregar] [Eliminar presupuesto]
  ↓
  Seccion MOVIMIENTOS
  ↓
  [Lista de expense cards]

EXPENSE CARD - Diseño de doble mitad:
  Parte superior:
    - Icono + descripcion + etiqueta (Compartido/Personal) + fecha
    - NUMERO GRANDE = mi_responsabilidad (lo que me toca pagar)
    - numero chico  = monto total del gasto
  Parte inferior (solo gastos compartidos):
    - Mitad izquierda (ELLOS): verde si su_parte_pagada=true
    - Mitad derecha (YO):      verde si mi_parte_pagada=true | boton "Pagar"
    - AnimatedContainer para transicion suave al verde
  Border completo verde cuando ambos han pagado.

LOGICA DE PAGO:
  mi_parte_pagada = true si:
    - Campo mi_parte_pagada=1 del API (nuevo campo)
    - O pagado_por == mi firebase_uid (yo pague el gasto completo)
  su_parte_pagada = true si:
    - Campo su_parte_pagada=1 del API (nuevo campo)
    - O pagado_por == otro uid (el otro pago el gasto completo)

ELIMINAR PRESUPUESTO:
  - Boton abre AlertDialog de confirmacion
  - Llama POST /shared-budgets/:id/request-delete
  - No elimina de inmediato: envía solicitud al co-dueño
  - Co-dueño debe aprobar para que se elimine
  - Columna delete_requested_by guarda quien solicito

APIs:
  GET  /shared-budgets/:id?firebase_uid=xxx
  GET  /shared-budgets/:id/expenses?firebase_uid=xxx
  POST /shared-budgets/:id/expenses
  POST /shared-expenses/:id/confirm-payment    ← NUEVO
  POST /shared-budgets/:id/request-delete       ← NUEVO
  DELETE /shared-expenses/:id?firebase_uid=xxx

────────────────────────────────────────────────────────
7.9 shared_budget_create.dart
────────────────────────────────────────────────────────
Tipo:       Screen
Funcion:    Crear presupuesto compartido con invitacion por email

Campos:
  - Nombre del presupuesto
  - Tipo de periodo (Mensual/Quincenal)
  - Regla de reparto:
      Equitativo (50/50)
      Porcentual (slider de %)
      Proporcional a ingresos (declarar ingreso mensual)
  - Email del co-dueño a invitar

APIs:
  POST /shared-budgets
  POST /shared-budgets/:id/invitations

────────────────────────────────────────────────────────
7.10 shared_budget_invitations.dart
────────────────────────────────────────────────────────
Tipo:       Screen
Funcion:    Gestionar invitaciones pendientes a presupuestos compartidos

Acciones:
  - Aceptar invitacion (con ingreso declarado si regla=proporcional)
  - Rechazar invitacion

APIs:
  GET  /shared-budget-invitations?firebase_uid=xxx
  POST /shared-budget-invitations/:token/accept
  POST /shared-budget-invitations/:token/reject

────────────────────────────────────────────────────────
7.11 ahorro_meta.dart
────────────────────────────────────────────────────────
(Sin cambios respecto a version anterior)

────────────────────────────────────────────────────────
7.12 progreso_ahorro.dart
────────────────────────────────────────────────────────
(Sin cambios respecto a version anterior)

────────────────────────────────────────────────────────
7.13 calendario.dart
────────────────────────────────────────────────────────
(Sin cambios respecto a version anterior)

================================================================================
7.14-7.20 MODULO OFRECIMIENTO DE SERVICIOS  [NUEVO 2026-05-11 sesion 2]
================================================================================

────────────────────────────────────────────────────────
7.14 ventas_landing_screen.dart
────────────────────────────────────────────────────────
Tipo:       Screen
Funcion:    Landing intermedio entre MainMenu y los dos modulos comerciales

Accede desde: MainMenu (tarjeta "Ventas")
Navega a:
  - CobrosHome (Venta de productos)
  - ServiciosDashboard (Ofrecimiento de servicios)

Elementos:
  - AppBar "Ventas" + boton info_outline
  - Titulo descriptivo
  - 2 tarjetas _DivisionCard con icono, titulo, subtitulo y chevron
  - Info dialog: explica las 2 divisiones

────────────────────────────────────────────────────────
7.15 servicios/servicios_dashboard.dart
────────────────────────────────────────────────────────
Tipo:       Screen
Funcion:    Lista de trabajos/proyectos del modulo servicios

Elementos:
  - AppBar + boton dashboard financiero (icono bar_chart) + boton info
  - FAB para crear nuevo trabajo
  - Lista de trabajos con:
      nombre, badge de estado (coloreado), nombre_cliente,
      barra de progreso cobro (total_recibido / monto_total),
      label de payment_status
  - Pull-to-refresh
  - Estado vacio con boton "Crear primer trabajo"

Colores de estado:
  draft=gris, pending=amarillo(primary), in_progress=azul(#0EA5E9),
  completed=verde(success), cancelled=rojo(danger)

APIs: GET /jobs?firebase_uid=xxx

────────────────────────────────────────────────────────
7.16 servicios/crear_trabajo_screen.dart
────────────────────────────────────────────────────────
Tipo:       Screen
Funcion:    Formulario para crear un nuevo trabajo/proyecto

Campos:
  - nombre (requerido)
  - nombre_cliente (requerido)
  - telefono_cliente (opcional)
  - descripcion (opcional, multilinea)
  - monto_total (requerido, numerico)
  - estado (dropdown: draft/pending/in_progress)
  - fecha_inicio / fecha_fin (DatePicker con tema oscuro)

Flujo: Al crear exitosamente navega con pushReplacement a TrabajoDetalleScreen
APIs: POST /jobs

────────────────────────────────────────────────────────
7.17 servicios/trabajo_detalle_screen.dart
────────────────────────────────────────────────────────
Tipo:       Screen (la mas compleja del modulo servicios)
Funcion:    Vista completa de un trabajo con 4 secciones colapsables

Secciones (ExpansionTile, initiallyExpanded: true):
  1. Pagos del cliente   → lista + boton + → RegistroPagoClienteScreen
  2. Colaboradores       → lista + boton + → bottom sheet agregar colaborador
  3. Gastos operativos   → lista + boton + → RegistroGastosScreen
  4. (header card siempre visible) Resumen financiero

Resumen financiero siempre visible:
  Total contratado / Total recibido / Saldo pendiente /
  Gastos operativos / Pagos colaboradores / Utilidad neta / Margen %

Header card:
  - cliente, telefono, descripcion
  - Dropdown de estado (cambia con PUT /jobs/:id)

Eliminar items: Dismissible con swipe izquierda → fondo rojo con icono delete

Bottom sheet colaboradores:
  - Campo nombre, dropdown tipo_compensacion (5 opciones), campo monto
  - Tipos: fijo, porcentual, por_horas, por_tarea, ganancias

APIs:
  GET  /jobs/:id?firebase_uid=xxx
  PUT  /jobs/:id (cambio de estado)
  POST /jobs/:id/team-members
  DELETE /jobs/:id/team-members/:mid
  DELETE /jobs/:id/customer-payments/:pid
  DELETE /jobs/:id/expenses/:eid

────────────────────────────────────────────────────────
7.18 servicios/registro_pago_cliente_screen.dart
────────────────────────────────────────────────────────
Tipo:       Screen
Funcion:    Registrar un pago recibido del cliente

Campos:
  - tipo (dropdown: anticipo / pago_parcial / pago_final)
  - monto (requerido, numerico)
  - nota (opcional)

Al guardar: actualiza automaticamente payment_status del job en el backend
APIs: POST /jobs/:id/customer-payments

────────────────────────────────────────────────────────
7.19 servicios/registro_gastos_screen.dart
────────────────────────────────────────────────────────
Tipo:       Screen
Funcion:    Registrar un gasto operativo del trabajo

Campos:
  - descripcion (requerido)
  - monto (requerido, numerico)
  - categoria (chips seleccionables: materiales/transporte/equipo/alimentacion/otro)

APIs: POST /jobs/:id/expenses

────────────────────────────────────────────────────────
7.20 servicios/financiero_dashboard_screen.dart
────────────────────────────────────────────────────────
Tipo:       Screen
Funcion:    Dashboard financiero global de todos los trabajos del usuario

Metricas globales (suma de todos los jobs):
  - Total contratado     = SUM(jobs.monto_total)
  - Total recibido       = SUM(customer_payments.monto)
  - Saldo pendiente      = total_contratado - total_recibido
  - Gastos operativos    = SUM(operational_expenses.monto)
  - Pagos colaboradores  = SUM(team_member_payments.monto)
  - Utilidad neta        = recibido - gastos - pagos_colab
  - Margen promedio      = promedio de margen de jobs con ingresos > 0

Layout:
  - Seccion Ingresos: 2 tarjetas (total contratado / total recibido) + 1 ancha (saldo)
  - Seccion Costos: 2 tarjetas (gastos / pagos colab)
  - Seccion Resultado: tarjeta grande utilidad + margen (borde verde/rojo segun valor)
  - Lista de trabajos individuales con recibido y utilidad por trabajo

APIs: GET /jobs?firebase_uid=xxx (reutiliza los datos agregados del listado)

================================================================================
8. SERVICIOS FLUTTER (lib/services/)
================================================================================

────────────────────────────────────────────────────────
api_client.dart
────────────────────────────────────────────────────────
Cliente HTTP centralizado.
  BASE_URL = https://presupuestos-backend-h3l6.onrender.com
  TIMEOUT  = 55 segundos
  Metodos estaticos: get(), post(), put(), delete()
  Headers: Content-Type: application/json

!! ADVERTENCIA CRITICA !!
  Durante desarrollo local, si se cambia baseUrl a 'http://localhost:3002'
  para probar, SIEMPRE revertirlo a la URL de Render antes de:
    1. Generar el APK de produccion
    2. Hacer git push (para que Render use el codigo correcto)
  El APK con localhost no funciona en dispositivos reales.

────────────────────────────────────────────────────────
shared_budget_service.dart
────────────────────────────────────────────────────────
CRUD completo de presupuestos compartidos.

Metodos:
  getAll(uid)                          → GET /shared-budgets
  getDetail(id, uid)                   → GET /shared-budgets/:id
  create(body)                         → POST /shared-budgets
  invite(budgetId, email, uid)         → POST /shared-budgets/:id/invitations
  getInvitations(uid)                  → GET /shared-budget-invitations
  acceptInvitation(token, uid, ingreso)→ POST /shared-budget-invitations/:token/accept
  rejectInvitation(token, uid)         → POST /shared-budget-invitations/:token/reject
  createExpense(budgetId, body)        → POST /shared-budgets/:id/expenses
  getExpenses(budgetId, uid)           → GET /shared-budgets/:id/expenses
  createSettlement(budgetId, body)     → POST /shared-budgets/:id/settlements
  getSettlements(budgetId, uid)        → GET /shared-budgets/:id/settlements
  deleteExpense(expenseId, uid)        → DELETE /shared-expenses/:id
  confirmarMiParte(expenseId, uid)     → POST /shared-expenses/:id/confirm-payment [NUEVO]
  requestDelete(budgetId, uid)         → POST /shared-budgets/:id/request-delete   [NUEVO]

────────────────────────────────────────────────────────
gustitos_service.dart  [NUEVO 2026-05-13]
────────────────────────────────────────────────────────
CRUD estatico del modulo Gustitos.

Metodos:
  listarPorPresupuesto(budgetId, uid) → GET /presupuestos/:id/gustitos
  resumen(budgetId, uid)              → GET /presupuestos/:id/gustitos/summary
  crear(body)                         → POST /gustitos
  eliminar(id, uid)                   → DELETE /gustitos/:id
  editar(id, body)                    → PATCH /gustitos/:id

────────────────────────────────────────────────────────
auth_service.dart
────────────────────────────────────────────────────────
  signIn()        → abre selector de cuenta Google
  silentSignIn()  → recupera sesion sin UI
  signOut()       → cierra sesion + limpia cache Hive
  currentUser     → getter del usuario actual

────────────────────────────────────────────────────────
cache_service.dart
────────────────────────────────────────────────────────
  init()           → abre box 'salarying_cache'
  set(key, data)   → guarda con timestamp
  get(key, maxAge) → retorna si edad < maxAge (default 24h)
  clearForUser(uid) → limpia datos del usuario al logout
  clear()          → limpia todo el cache

================================================================================
9. TEMA Y WIDGETS
================================================================================

────────────────────────────────────────────────────────
app_theme.dart — Paleta inspirada en Binance
────────────────────────────────────────────────────────
Fondos:
  background    #0B0E11   Negro profundo (fondo global)
  surface       #1E2026   Tarjetas, modales
  surfaceAlt    #2B3139   Inputs, fondos secundarios
  border        #2B3139

Acentos:
  primary       #F0B90B   Amarillo Binance (botones principales)

Texto:
  textPrimary   #EAECEF
  textSecondary #848E9C
  textMuted     #5E6673

Estados:
  success       #0ECB81   Verde
  danger        #F6465D   Rojo
  info          #2E8DEF   Azul

Colores por tipo de gasto:
  colorFijo     #1890FF   Azul
  colorNoFijo   #F0B90B   Amarillo (variable)
  colorAhorro   #0ECB81   Verde

================================================================================
10. BACKEND - ENDPOINTS REST
================================================================================

Base URL: https://presupuestos-backend-h3l6.onrender.com

────────────────────────────────────────────────────────
10.1 PRESUPUESTOS
────────────────────────────────────────────────────────
GET /presupuestos?firebase_uid=xxx
  Retorna: array con campos basicos del presupuesto
  CAMPOS DISPONIBLES: id, nombre, monto_total, firebase_uid,
                      tipo_periodo, dia_inicio_periodo
  CAMPOS NO DISPONIBLES: gastado, disponible, fecha_inicio, fecha_fin
  → Para obtener gastado real usar GET /presupuestos/:id/detalle

GET /presupuestos/:id/detalle?firebase_uid=xxx
  Retorna:
    {
      periodo: { id, fecha_inicio, fecha_fin, estado },
      movimientos: [{ id, descripcion, monto, tipo, pagado, monto_pagado_real,
                      subcategoria }],   ← NUEVO campo
      resumen: { totalFijo, totalNoFijo, totalAhorro, totalGastado,
                 totalGustitos,          ← NUEVO campo
                 balance_disponible,     ← ahora = monto_total - totalGastado - totalGustitos
                 porcentaje_utilizado, porcentajePagados }
    }
  NOTA: totalGastado = suma de m.monto (montos presupuestados, no lo pagado real)
  Para gastado real: SUM(monto_pagado_real) WHERE pagado=1 desde movimientos
  NOTA: balance_disponible ya descuenta los Gustitos del periodo

GET /presupuestos/:id/historico?firebase_uid=xxx
  Ultimos 6 periodos.
  Retorna: { monto_total, periodos: [{ total_gastado, movimientos_pagados, ... }] }
  NOTA: En historico si, total_gastado = SUM(monto_pagado_real) (el real)

POST /presupuestos
PUT /presupuestos/:id

────────────────────────────────────────────────────────
10.2 GASTOS Y MOVIMIENTOS  [ACTUALIZADO 2026-05-13]
────────────────────────────────────────────────────────
POST /gastos
  Body: { ..., subcategoria? }   ← NUEVO campo opcional
  subcategoria: string libre, ej: 'gasolina', 'supermercado', 'educacion'
  Se propaga automaticamente al movimiento auto-creado para fijo/ahorro

POST /gastos/ahorroMeta
GET /presupuestos/:id/gastos?firebase_uid=xxx
  Retorna: incluye subcategoria (NULL si no fue asignada)

GET /presupuestos/:id/gastos-seleccionables?firebase_uid=xxx
PUT /gastos/:id
PUT /presupuestos/:id/gastos/reanudar-fijos
DELETE /gastos/:id?firebase_uid=xxx

POST /presupuestos/:id/movimientos
  Items: [{ gasto_id, monto, subcategoria? }]   ← subcategoria por item NUEVO
  Si subcategoria no viene en el item, hereda la del gasto

PUT /movimientos/:id/pagar   Body: { pagado, monto_pagado_real, firebase_uid }

SUBCATEGORIAS SUGERIDAS (validacion en frontend, backend acepta VARCHAR libre):
  supermercado, gasolina, educacion, entretenimiento, salud, restaurantes,
  ropa, transporte, mantenimiento, tecnologia, hogar, otros

────────────────────────────────────────────────────────
10.9 GUSTITOS  [NUEVO 2026-05-13]
────────────────────────────────────────────────────────
Un Gustito es una compra pequena, espontanea o personal registrada con
conciencia financiera sin generar culpa psicologica.
Afecta directamente el disponible del presupuesto.
Preparado para integracion futura con facturas QR (scanned_invoice_id).

Regla financiera:
  availableAmount = budgetTotal - paidExpenses - totalGustitos

IMPORTANTE: Nunca usar "gasto hormiga" en la UI. Siempre usar "Gustito/Gustitos".

POST /gustitos
  Body: { user_id, budget_id, name, amount, spent_at,
          description?, merchant?, category?, emotion_tag?, source? }
  Requeridos: user_id (firebase_uid), budget_id, name, amount > 0, spent_at
  Valida que budget_id pertenece a user_id
  source ENUM: manual | scanned_invoice | imported  (default: manual)
  emotion_tag ENUM: antojo | premio | social | impulso | estres | otro
  category: string libre (mismas opciones que subcategoria de gastos)
  Retorna: registro completo creado

GET /gustitos?firebase_uid=xxx
  Lista todos los Gustitos del usuario (deleted_at IS NULL), orden spent_at DESC

GET /gustitos/:id?firebase_uid=xxx
  Detalle de un Gustito. 404 si no existe o no pertenece al usuario.

PATCH /gustitos/:id
  Body: { firebase_uid, name?, amount?, description?, merchant?,
          category?, emotion_tag?, spent_at? }
  Solo actualiza los campos enviados. Construye SET dinamico.
  Retorna: registro actualizado

DELETE /gustitos/:id?firebase_uid=xxx
  Soft delete: SET deleted_at = NOW()
  El Gustito deja de aparecer en listados y ya no descuenta del disponible.

GET /presupuestos/:budgetId/gustitos?firebase_uid=xxx
  Lista Gustitos de un presupuesto especifico (deleted_at IS NULL), orden DESC

GET /presupuestos/:budgetId/gustitos/summary?firebase_uid=xxx
  Retorna:
    {
      total:               decimal (suma de amounts),
      count:               int,
      promedio:            decimal,
      categoria_frecuente: string | null,
      dia_con_mas:         date | null,
      ultimos:             [ ...3 registros recientes ]
    }

────────────────────────────────────────────────────────
10.3 AHORROS
────────────────────────────────────────────────────────
GET /ahorros?firebase_uid=xxx
  Retorna: [{ id, nombre, monto_meta, monto_ahorrado, total_aportaciones }]
  CAMPOS EXACTOS:
    nombre           → descripcion del gasto de ahorro
    monto_meta       → meta total (NO se llama 'monto')
    monto_ahorrado   → suma de monto_pagado_real de movimientos pagados
    total_aportaciones → suma de aportaciones manuales adicionales
  TOTAL REAL AHORRADO = monto_ahorrado + total_aportaciones

POST /ahorros/:id/aportaciones
GET /ahorros/:id/aportaciones?firebase_uid=xxx
DELETE /aportaciones/:id?firebase_uid=xxx
DELETE /ahorros/:id?firebase_uid=xxx

────────────────────────────────────────────────────────
10.4 CALENDARIO
────────────────────────────────────────────────────────
GET /calendario/eventos?firebase_uid=xxx&mes=M&anio=A
  Retorna: [{ id, titulo, tipo, fecha_evento, monto_esperado, estado }]
  tipo: 'pago' (gasto) o 'cobro' (ingreso de venta)
  estado: 'pendiente' | 'pagado' | 'cobrado' | 'vencido'

PUT /calendario/eventos/:id/estado
DELETE /calendario/eventos/:id?firebase_uid=xxx&solo_este=true|false

────────────────────────────────────────────────────────
10.5 VENTAS Y COBROS
────────────────────────────────────────────────────────
GET /ventas?firebase_uid=xxx
  Retorna: v.* + total_cobrado, total_esperado, cobros_pendientes
  total_cobrado = SUM(cobros_clientes.monto_cobrado WHERE estado='cobrado')
  NOTA: ventas.estado puede ser 'activa', filtrarlo en Flutter si es necesario

GET /ventas/:id?firebase_uid=xxx → detalle completo con metricas
POST /ventas
PUT /ventas/:id
DELETE /ventas/:id?firebase_uid=xxx
POST /ventas/:id/cobros
PUT /cobros/:id/cobrar
DELETE /cobros/:id?firebase_uid=xxx
GET /ventas/:id/lista-compras?firebase_uid=xxx

────────────────────────────────────────────────────────
10.6 PRESUPUESTOS COMPARTIDOS
────────────────────────────────────────────────────────
GET  /shared-budgets?firebase_uid=xxx
GET  /shared-budgets/:id?firebase_uid=xxx
POST /shared-budgets
POST /shared-budgets/:id/invitations
GET  /shared-budget-invitations?firebase_uid=xxx
POST /shared-budget-invitations/:token/accept
POST /shared-budget-invitations/:token/reject

GET  /shared-budgets/:id/expenses?firebase_uid=xxx        [ACTUALIZADO]
  Retorna campos adicionales:
    mi_parte_pagada  → 1 si yo pague mi parte (pagado_por=yo OR shared_expense_splits.pagado=1)
    su_parte_pagada  → 1 si el otro pago su parte (pagado_por=otro OR otro_split.pagado=1)
    mi_responsabilidad → monto que me corresponde pagar

POST /shared-budgets/:id/expenses
DELETE /shared-expenses/:id?firebase_uid=xxx

POST /shared-expenses/:id/confirm-payment                 [NUEVO]
  Body: { firebase_uid }
  Funcion: marca shared_expense_splits.pagado=1 para el usuario
  Verifica que el usuario sea miembro del presupuesto
  Respuesta 200: { message: 'Pago confirmado' }

POST /shared-budgets/:id/settlements
GET  /shared-budgets/:id/settlements?firebase_uid=xxx

POST /shared-budgets/:id/request-delete                   [NUEVO]
  Body: { firebase_uid }
  Funcion: guarda firebase_uid en shared_budgets.delete_requested_by
  El presupuesto no se elimina aun. El otro usuario debe aprobar.
  Respuesta 200: { message: 'Solicitud de eliminacion enviada' }

────────────────────────────────────────────────────────
10.7 PRODUCTOS, RECETAS, PRODUCCION
────────────────────────────────────────────────────────
(Sin cambios, ver version anterior del documento)

────────────────────────────────────────────────────────
10.8 OFRECIMIENTO DE SERVICIOS (JOBS)  [NUEVO 2026-05-11 sesion 2]
────────────────────────────────────────────────────────

GET /jobs?firebase_uid=xxx
  Retorna: array de jobs con campos agregados:
    id, firebase_uid, nombre, descripcion, nombre_cliente, telefono_cliente,
    monto_total, estado, payment_status, fecha_inicio, fecha_fin, created_at,
    total_recibido (SUM customer_payments),
    total_gastos (SUM operational_expenses),
    total_pagos_colaboradores (SUM team_member_payments)

POST /jobs
  Body: { firebase_uid, nombre, nombre_cliente, telefono_cliente?,
          descripcion?, monto_total, estado?, fecha_inicio?, fecha_fin? }
  Requeridos: firebase_uid, nombre, monto_total
  Retorna: job recien creado
  Efecto: crea registro en job_activity_logs (accion='created')

GET /jobs/:id?firebase_uid=xxx
  Retorna objeto completo:
    {
      job: { ...campos del trabajo },
      teamMembers: [...],
      customerPayments: [...],
      expenses: [...],
      teamPayments: [{ ...pago, nombre_colaborador }],
      resumen: {
        monto_total, total_recibido, saldo_pendiente,
        total_gastos, total_pagos_colaboradores,
        utilidad_neta, margen (%)
      }
    }
  Formula utilidad_neta = total_recibido - total_gastos - total_pagos_colaboradores

PUT /jobs/:id
  Body: { firebase_uid, nombre?, descripcion?, nombre_cliente?,
          telefono_cliente?, monto_total?, estado?, payment_status?,
          fecha_inicio?, fecha_fin? }
  Usa COALESCE para actualizar solo campos enviados
  Si se envia estado: registra en job_activity_logs (accion='status_change')

DELETE /jobs/:id?firebase_uid=xxx
  Elimina trabajo y en cascada todos sus registros relacionados

POST /jobs/:id/team-members
  Body: { firebase_uid, nombre, tipo_compensacion,
          monto_acordado?, porcentaje?, horas_trabajadas?, tarifa_hora? }
  tipo_compensacion ENUM: fijo | porcentual | por_horas | por_tarea | ganancias
  Calcula monto_calculado automaticamente:
    fijo     → monto_acordado
    por_horas → horas_trabajadas × tarifa_hora

DELETE /jobs/:id/team-members/:mid?firebase_uid=xxx

POST /jobs/:id/customer-payments
  Body: { firebase_uid, monto, tipo?, nota? }
  tipo ENUM: anticipo | pago_parcial | pago_final
  Efectos:
    1. Inserta pago en customer_payments
    2. Recalcula payment_status del job:
       recibido >= monto_total → 'paid'
       recibido > 0            → 'partially_paid'
       recibido = 0            → 'pending'
    3. Registra en job_activity_logs (accion='customer_payment')

DELETE /jobs/:id/customer-payments/:pid?firebase_uid=xxx

POST /jobs/:id/expenses
  Body: { firebase_uid, descripcion, monto, categoria? }
  categoria libre: materiales | transporte | equipo | alimentacion | otro
  Registra en job_activity_logs (accion='expense')

DELETE /jobs/:id/expenses/:eid?firebase_uid=xxx

POST /jobs/:id/team-member-payments
  Body: { firebase_uid, team_member_id, monto, nota? }
  Registra en job_activity_logs (accion='team_payment')
  Retorna pago con nombre_colaborador del JOIN

GET /jobs/:id/financial-summary?firebase_uid=xxx
  Retorna resumen financiero puntual del trabajo (igual que resumen en GET /jobs/:id)

================================================================================
11. BASE DE DATOS Y BACKEND CONFIG
================================================================================

CONEXION MYSQL:
  Host:     bvmmbs7zrnufnskqoxwm-mysql.services.clever-cloud.com
  Puerto:   3306
  Database: bvmmbs7zrnufnskqoxwm
  Pool:     max 3 conexiones  ← CRITICO: Clever Cloud free = max 5 por usuario
                                  Usar 3 deja margen para conexiones administrativas

!! REGLA OBLIGATORIA !!
  connectionLimit NUNCA debe superar 3 en produccion con Clever Cloud free tier.
  Si se sube a 10, al reiniciar Render ocurre ER_USER_LIMIT_REACHED y el backend
  puede quedar degradado aunque el log diga "Your service is live".
  Si el servidor local esta corriendo (node server.js), MATARLO antes de que
  Render haga deploy, de lo contrario ambos consumen las 5 conexiones disponibles.

VARIABLES DE ENTORNO (.env):
  MYSQLDATABASE   bvmmbs7zrnufnskqoxwm
  MYSQLHOST       bvmmbs7zrnufnskqoxwm-mysql.services.clever-cloud.com
  MYSQLPASSWORD   [credenciales en keystore-credentials.enc]
  MYSQLPORT       3306
  MYSQLUSER       ueuaxoqgtveollq5
  PORT            3002

CRON JOB:
  Schedule:  0 0 * * * (medianoche, America/Panama UTC-5)
  Funcion:   Marca como 'vencido' eventos calendario pendientes con fecha pasada

TABLAS COMPARTIDAS (shared_budgets module):
  shared_budgets              → presupuestos compartidos
    Columna nueva: delete_requested_by VARCHAR(255) DEFAULT NULL
  shared_budget_members       → miembros con porcentaje e ingreso
  shared_budget_invitations   → invitaciones por email con token
  shared_expenses             → gastos del presupuesto compartido
  shared_expense_splits       → responsabilidad por usuario
    Columna: pagado TINYINT(1) DEFAULT 0  ← activa desde 2026-05-11
  shared_settlements          → pagos de liquidacion entre usuarios
  shared_budget_activity_logs → historial de acciones

TABLAS SERVICIOS (jobs module) [NUEVO 2026-05-11 sesion 2]:
  jobs                  → trabajo/proyecto principal
    id, firebase_uid, nombre, descripcion, nombre_cliente, telefono_cliente,
    monto_total, estado ENUM(draft|pending|in_progress|completed|cancelled),
    payment_status ENUM(pending|partially_paid|paid),
    fecha_inicio, fecha_fin, created_at
  job_team_members      → colaboradores asignados al trabajo
    id, job_id, firebase_uid, nombre, tipo_compensacion ENUM(5 tipos),
    monto_acordado, porcentaje, horas_trabajadas, tarifa_hora, monto_calculado
    FK: job_id → jobs(id) ON DELETE CASCADE
  customer_payments     → pagos recibidos del cliente
    id, job_id, firebase_uid, monto, tipo ENUM(anticipo|pago_parcial|pago_final),
    nota, fecha_pago
    FK: job_id → jobs(id) ON DELETE CASCADE
  team_member_payments  → pagos realizados a colaboradores
    id, job_id, team_member_id, firebase_uid, monto, nota, fecha_pago
    FK: job_id → jobs(id) ON DELETE CASCADE
    FK: team_member_id → job_team_members(id) ON DELETE CASCADE
  operational_expenses  → gastos operativos del trabajo
    id, job_id, firebase_uid, descripcion, monto, categoria, fecha_gasto
    FK: job_id → jobs(id) ON DELETE CASCADE
  job_activity_logs     → historial de acciones del trabajo
    id, job_id, firebase_uid, accion, detalle, created_at
    FK: job_id → jobs(id) ON DELETE CASCADE

TABLAS GUSTITOS (modulo Gustitos) [NUEVO 2026-05-13]:
  gustitos
    id                 INT AUTO_INCREMENT PK
    user_id            VARCHAR(128) NOT NULL          ← firebase_uid
    budget_id          INT NOT NULL                   ← FK presupuestos(id) CASCADE
    scanned_invoice_id INT NULL                       ← preparado para QR futuro
    name               VARCHAR(255) NOT NULL
    description        TEXT NULL
    merchant           VARCHAR(255) NULL
    amount             DECIMAL(10,2) NOT NULL
    category           VARCHAR(100) NULL
    emotion_tag        ENUM(antojo|premio|social|impulso|estres|otro) NULL
    source             ENUM(manual|scanned_invoice|imported) DEFAULT manual
    spent_at           DATE NOT NULL
    created_at         TIMESTAMP DEFAULT CURRENT_TIMESTAMP
    updated_at         TIMESTAMP ON UPDATE CURRENT_TIMESTAMP
    deleted_at         TIMESTAMP NULL                 ← soft delete
    INDEX: idx_gustitos_user(user_id), idx_gustitos_budget(budget_id)

COLUMNAS NUEVAS EN TABLAS EXISTENTES [2026-05-13]:
  gastos.subcategoria      VARCHAR(100) NULL DEFAULT NULL
  movimientos.subcategoria VARCHAR(100) NULL DEFAULT NULL
  Comportamiento: nullable, compatibles con todos los registros previos.
  La subcategoria se hereda del gasto al movimiento si no se especifica en el item.

MIGRACION EJECUTADA (2026-05-13):
  database/migrations/add_gustitos_subcategorias.sql → ejecutado en Clever Cloud OK
  Incluye: ALTER gastos, ALTER movimientos, CREATE TABLE gustitos.

MIGRACION EJECUTADA (2026-05-11 sesion 2):
  database/migrations/add_servicios_jobs.sql → ejecutado en Clever Cloud OK
  Las 6 tablas del modulo servicios ya existen en produccion.

MIGRACION PENDIENTE (si no se ha ejecutado):
  ALTER TABLE shared_budgets
    ADD COLUMN delete_requested_by VARCHAR(255) DEFAULT NULL;

================================================================================
12. CONFIGURACION ANDROID
================================================================================

PERMISOS (AndroidManifest.xml):
  INTERNET, RECEIVE_BOOT_COMPLETED, POST_NOTIFICATIONS, SCHEDULE_EXACT_ALARM

BUILD (android/app/build.gradle):
  ndkVersion = flutter.ndkVersion   → requiere NDK instalado en SDK
  NDK version: 26.3.11579264        → instalar con sdkmanager si se elimina

KEYSTORE:
  Archivo:  android/app/release.jks
  Alias:    salarying
  Comando de firma: automatico via key.properties en build.gradle

================================================================================
13. GENERAR APK - PROCESO CORRECTO
================================================================================

REQUISITOS ANTES DE GENERAR:
  1. Verificar que baseUrl en lib/services/api_client.dart apunte a Render:
       static const String baseUrl = 'https://presupuestos-backend-h3l6.onrender.com';
     (Si estaba en localhost durante desarrollo, REVERTIR antes de build)

  2. Espacio en disco: necesita minimo 5 GB libres en C:
     - Si hay menos espacio: limpiar Downloads, Papelera
     - NO borrar: Pub Cache, NDK, Flutter SDK (rompe el build)
     - Se puede borrar: .gradle/caches (se reconstruye), system-images del emulador

  3. Matar servidor local si esta corriendo (libera conexiones MySQL)

COMANDO:
  flutter build apk --release

SALIDA:
  build/app/outputs/flutter-apk/app-release.apk  (~24 MB)

ALTERNATIVA (si hay poco espacio, solo arm64):
  flutter build apk --debug --split-per-abi --target-platform android-arm64
  Salida: app-arm64-v8a-debug.apk (~47 MB, solo para debugging)

ERRORES COMUNES AL BUILDEAR:
  "symlink support" → activar Developer Mode en Windows (Configuracion > Para desarrolladores)
  "Espacio insuficiente" → liberar al menos 5 GB en C:
  "NDK did not have source.properties" → reinstalar NDK via sdkmanager:
    $env:JAVA_HOME = "C:\Program Files\Android\Android Studio\jbr"
    $env:PATH = "$env:JAVA_HOME\bin;$env:PATH"
    sdkmanager.bat "ndk;26.3.11579264"
  "CorruptedCacheException" → matar java/gradle + borrar .gradle + volver a buildear

================================================================================
14. BUGS CORREGIDOS Y NOTAS TECNICAS
================================================================================

BUG 1: Cobrado desde Calendario no actualizaba VentaDetalle
  Solucion: routeObserver global + RouteAware en VentaDetalle (didPopNext)

BUG 2: Fechas en Calendario con timezone
  Solucion: comparar fechas como string "YYYY-MM-DD" con _toDateStr()

BUG 3: Fondo gris en Android Release mode
  Solucion: Container explicito con color: AppTheme.background en estados vacios

BUG 4: Parsing robusto de numeros MySQL
  Solucion: double.tryParse(v?.toString() ?? '0') ?? 0.0 en todos los parseos

BUG 5: Movimiento creado sin ID de gasto
  Solucion: Validar id != null antes de crear movimiento

BUG 6: Estado invalido al cobrar desde calendario
  Solucion: Usar siempre 'pagado' (no 'cobrado') para eventos calendario

BUG 7: Dashboard mostraba $0 en gastado y fechas vacias [2026-05-11]
  Problema: GET /presupuestos no retorna gastado, fecha_inicio, fecha_fin
  Solucion: Llamar GET /presupuestos/:id/detalle despues de obtener el ID activo
            y calcular: gastado = SUM(movimiento.monto_pagado_real WHERE pagado=1)

BUG 8: Dashboard mostraba $0 en ahorro [2026-05-11]
  Problema: Nombres de campos incorrectos. La API retorna 'monto_meta' y
            'monto_ahorrado', no 'monto' y 'total_ahorrado'
  Solucion: Corregir field names en _cargarAhorro() y _buildAhorroCard()
  REGLA: siempre verificar nombres exactos de campos contra el SQL del backend

BUG 9: ER_USER_LIMIT_REACHED al redesplegar en Render [2026-05-11]
  Problema: connectionLimit=10 excede el maximo de 5 de Clever Cloud free tier
  Solucion: Reducir connectionLimit a 3 en server.js
  REGLA: Ademas matar el servidor local antes de que Render despliegue

BUG 10: APK apuntaba a localhost despues de pruebas locales [2026-05-11]
  Problema: Se cambio baseUrl a localhost para pruebas y no se revirtio antes del APK
  Solucion: Siempre verificar baseUrl en api_client.dart antes de generar APK

BUG 11: shared_expense_splits.pagado existia pero nunca se usaba [2026-05-11]
  Problema: El campo 'pagado' en la tabla de splits estaba en el schema pero
            ningun endpoint lo leia ni actualizaba, por eso el boton Pagar fallaba
  Solucion: Nuevo endpoint POST /shared-expenses/:id/confirm-payment que hace
            UPDATE shared_expense_splits SET pagado=1 WHERE expense_id=? AND firebase_uid=?
            El GET /expenses ahora retorna mi_parte_pagada y su_parte_pagada

================================================================================
15. DEPENDENCIAS
================================================================================

FLUTTER (pubspec.yaml):
  flutter, http ^1.2.0, intl ^0.19.0, table_calendar ^3.1.2
  flutter_local_notifications ^17.2.4, cupertino_icons ^1.0.8
  google_sign_in ^6.2.1, hive ^2.2.3, hive_flutter ^1.1.0
  pdf ^3.10.8, printing ^5.12.0
  Dev: flutter_test, flutter_lints ^4.0.0

NODE.JS (backend/package.json):
  express ^4.21.0, mysql2 ^3.16.0, cors ^2.8.5
  node-cron ^3.0.3, node-fetch ^3.3.2
  Node.js requerido: 22.x (Render usa v22.22.0)

================================================================================
16. INFRAESTRUCTURA DE PRODUCCION
================================================================================

RENDER (backend):
  URL:     https://presupuestos-backend-h3l6.onrender.com
  Branch:  main (deploy automatico en push)
  Build:   npm install (en directorio backend/)
  Start:   node server.js
  Plan:    Gratuito (duerme tras inactividad, 50s para despertar)

CLEVER CLOUD (MySQL):
  Plan:    MySQL compartido gratuito
  Limite:  5 conexiones simultaneas por usuario
  Pool:    connectionLimit=3 en server.js

FIREBASE:
  Proyecto: salary-bi (ID: 750057422826)
  Servicio: Google Sign-In (OAuth para Android)

APK RELEASE:
  Ruta:    build/app/outputs/flutter-apk/app-release.apk
  Tamaño:  ~24 MB
  Firmado: release.jks (alias: salarying)

================================================================================
17. SESION 2026-05-11 - RESUMEN DE CAMBIOS Y LECCIONES
================================================================================

CAMBIOS IMPLEMENTADOS:
  1. main_menu.dart      → Card expandible Presupuesto (Individual/Compartido)
  2. shared_budget_detail.dart → Rediseño completo: scroll unico, split de pagos verde
  3. dashboard_screen.dart → Dashboard Harvard: 7 cards, score financiero, ritmo
  4. shared_budget_service.dart → confirmarMiParte(), requestDelete()
  5. server.js           → POST confirm-payment, POST request-delete,
                           GET expenses retorna mi_parte_pagada/su_parte_pagada,
                           connectionLimit 10→3
  6. migration_shared_budgets.sql → ALTER TABLE add delete_requested_by
  7. api_client.dart     → baseUrl verificado en Render (no localhost)

COMMITS EN ESTA SESION:
  479924f feat: shared budget payments, expandable menu, Harvard dashboard
  d7746f3 fix: split proximos 7 dias card (Gastos/Ingresos con colores)
  c73e48a fix: dashboard field names (monto_meta, monto_ahorrado, detalle endpoint)
  1172e36 fix: reduce MySQL pool connectionLimit 10→3 Clever Cloud

LECCIONES APRENDIDAS:
  - Siempre verificar nombres exactos de campos contra el SQL real del backend
    antes de escribir codigo Flutter que lee esa data
  - El GET /presupuestos (lista) NO tiene gastado. Usar GET /presupuestos/:id/detalle
  - Ahorros: los campos son monto_meta y monto_ahorrado (no monto y total_ahorrado)
  - shared_expense_splits.pagado existia en schema pero sin uso: verificar siempre
    que los campos del schema esten realmente siendo leidos/escritos en los endpoints
  - connectionLimit de mysql2 debe estar por debajo del limite del plan de BD
  - Matar procesos locales antes de deploy a Render (comparten conexiones MySQL)
  - Nunca borrar Pub Cache, NDK o Flutter SDK para liberar espacio (rompe builds)
  - Mantener al menos 5 GB libres en disco para generar APKs
  - Siempre revertir baseUrl a produccion antes de buildear APK de release

================================================================================
18. SESION 2026-05-11 (sesion 2) - MVP MODULO OFRECIMIENTO DE SERVICIOS
================================================================================

OBJETIVO:
  Reestructurar el modulo Cobros en dos divisiones comerciales independientes:
    1. Venta de productos  → CobrosHome existente (sin cambios internos)
    2. Ofrecimiento de servicios → nuevo modulo completo (jobs)

CAMBIOS IMPLEMENTADOS:
  1. main_menu.dart
       - Tarjeta "Cobros" renombrada a "Ventas" (icono storefront)
       - Navegacion cambia de CobrosHome a VentasLandingScreen
       - Texto del info dialog actualizado

  2. ventas_landing_screen.dart (NUEVO)
       - Landing con 2 tarjetas _DivisionCard
       - Boton info_outline con explicacion de las 2 divisiones

  3. servicios/ (directorio NUEVO con 6 pantallas)
       - servicios_dashboard.dart
       - crear_trabajo_screen.dart
       - trabajo_detalle_screen.dart
       - registro_pago_cliente_screen.dart
       - registro_gastos_screen.dart
       - financiero_dashboard_screen.dart

  4. services/servicios_service.dart (NUEVO)
       - 11 metodos estaticos para CRUD completo del modulo jobs

  5. backend/server.js
       - +12 rutas nuevas agrupadas bajo /jobs
       - payment_status se recalcula automaticamente al registrar pago cliente
       - job_activity_logs registra cada accion importante

  6. database/migrations/add_servicios_jobs.sql (NUEVO)
       - 6 tablas con IF NOT EXISTS y FK CASCADE
       - EJECUTADO exitosamente en Clever Cloud (2026-05-11)

  7. APK generado: build/app/outputs/flutter-apk/app-release.apk (24.4 MB)

PATRON DE BOTONES INFO:
  TODAS las pantallas del modulo servicios tienen boton info_outline en el AppBar.
  Patron identico al resto de la app: showDialog → AlertDialog con
  backgroundColor: AppTheme.surface, borderRadius: 12, accion "Entendido".

MODELO FINANCIERO IMPLEMENTADO:
  Ingreso bruto (total_recibido)
  - Gastos operativos (operational_expenses)
  - Pagos a colaboradores (team_member_payments)
  = Utilidad neta
  Margen % = (utilidad_neta / total_recibido) * 100

REGLAS DE NEGOCIO IMPORTANTES:
  - customer_payments y team_member_payments NUNCA se mezclan en calculos
  - Cada pago del cliente actualiza automaticamente el payment_status del job
  - Todos los DELETE son en cascada desde jobs
  - firebase_uid requerido en todos los endpoints (GET query / POST/PUT/DELETE body)

LECCIONES APRENDIDAS:
  - El modulo CobrosHome no fue tocado internamente: VentasLandingScreen
    actua como capa de navegacion sin modificar la logica existente
  - mysql CLI en Windows permite ejecutar migraciones directamente desde la
    terminal sin necesidad de entrar al panel de Clever Cloud
  - El unused_import en main_menu.dart (cobros_home.dart) fue el unico warning
    nuevo introducido, corregido de inmediato

================================================================================
19. SESION 2026-05-12 - FIX: COMMIT MODULO SERVICIOS (JOBS)
================================================================================

PROBLEMA DETECTADO:
  Error al crear trabajo: 404 en POST /jobs desde la app.

CAUSA RAIZ:
  La sesion 2 del 2026-05-11 (modulo Ofrecimiento de Servicios) implemento
  12 rutas en server.js y 6 pantallas Flutter, pero NINGUN archivo fue commiteado
  ni pusheado a GitHub. Render deployó el ultimo commit (7b7e515) que no
  incluia esas rutas → 404 en todos los endpoints /jobs.

ARCHIVOS COMMITEADOS EN ESTA SESION (lo que faltaba de la sesion anterior):
  backend/server.js                          → +12 rutas /jobs
  lib/main_menu.dart                         → Ventas → VentasLandingScreen
  lib/ventas_landing_screen.dart             → NUEVO
  lib/services/servicios_service.dart        → NUEVO
  lib/servicios/servicios_dashboard.dart     → NUEVO
  lib/servicios/crear_trabajo_screen.dart    → NUEVO
  lib/servicios/trabajo_detalle_screen.dart  → NUEVO
  lib/servicios/registro_pago_cliente_screen.dart → NUEVO
  lib/servicios/registro_gastos_screen.dart  → NUEVO
  lib/servicios/financiero_dashboard_screen.dart  → NUEVO
  lib/widgets/ventas/margen_objetivo_card.dart    → NUEVO
  database/migrations/add_servicios_jobs.sql      → NUEVO
  android/app/google-services.json               → google-services.json ahora presente
  android/app/build.gradle                       → actualizado

BUG 12: POST /jobs retorna 404 [2026-05-12]
  Problema: Las rutas /jobs existian en server.js local pero nunca fueron
            commiteadas. Render servia el codigo sin esas rutas.
  Solucion: Commit + push de server.js y todos los archivos del modulo servicios.
  Leccion:  Siempre verificar con "git status" antes de cerrar sesion para
            asegurar que TODO el codigo nuevo quedo commiteado y pusheado.
            El APK puede estar en produccion pero si el backend no fue pusheado,
            la app fallara con 404 en los nuevos endpoints.

ESTADO POST-FIX:
  - Render auto-redespliega al recibir el push (1-3 minutos)
  - Todos los endpoints /jobs quedan activos en produccion
  - APK existente (24.4 MB, sesion 2026-05-11 sesion 2) sigue siendo valido
    (el codigo Flutter ya tenia las pantallas del modulo servicios)

================================================================================
20. SESION 2026-05-12 (sesion 2) - PULIDO MODULO OFRECIMIENTO DE SERVICIOS
================================================================================

OBJETIVO:
  Corregir gaps funcionales del modulo de servicios y agregar proyeccion de ganancia.

BUG 13: No habia UI para registrar pago a colaborador [2026-05-12]
  Problema: El endpoint POST /jobs/:id/team-member-payments existia en backend
            y addTeamMemberPayment() existia en ServiciosService, pero ningun
            widget Flutter llamaba a ese metodo. El usuario podia crear
            colaboradores pero no registrar pagos hacia ellos.
  Solucion: Nuevo metodo _pagarColaborador() en trabajo_detalle_screen.dart
            que abre un BottomSheet con monto pre-relleno y nota opcional.
            Tambien se agrego _eliminarPagoColab() y el endpoint DELETE en backend.

CAMBIOS IMPLEMENTADOS:

  1. backend/server.js — nuevo endpoint:
       DELETE /jobs/:id/team-member-payments/:pid
         - Valida que el job pertenece al firebase_uid
         - Elimina el pago de team_member_payments
         - Retorna { message: 'Pago eliminado' }

  2. lib/services/servicios_service.dart — nuevo metodo:
       deleteTeamMemberPayment(jobId, paymentId, firebaseUid)
         → DELETE /jobs/:id/team-member-payments/:pid

  3. lib/servicios/trabajo_detalle_screen.dart — refactorizacion completa:

     _colaboradorTile() REFACTORIZADO:
       - Ahora recibe montoTotal como parametro
       - Muestra badge de estado: Pendiente (rojo) / Parcial (amarillo) / Pagado (verde)
       - Boton + (add_circle_outline) para abrir BottomSheet de pago
       - Sub-lista de pagos registrados con boton X para eliminar cada uno
       - Badge desaparece cuando el colaborador esta completamente pagado

     _pagarColaborador() NUEVO:
       - BottomSheet con monto pre-relleno al saldo pendiente
       - Muestra: acordado, ya pagado, pendiente como contexto
       - Campo nota opcional
       - Llama ServiciosService.addTeamMemberPayment()

     _pagoColabSubTile() NUEVO:
       - Muestra cada pago registrado al colaborador bajo su tile
       - Boton X (Icons.close) para eliminar el pago
       - Muestra nota y monto en verde

     _calcMontoColaborador() NUEVO:
       - Helper para calcular compensacion esperada segun tipo_compensacion:
           fijo/por_horas/por_tarea → monto_calculado del backend
           porcentual              → montoTotal * porcentaje / 100
           ganancias               → 0 (circular, excluido de proyeccion)

     _labelComp() MEJORADO:
       - Ahora recibe Map<String, dynamic> en lugar de solo tipo+monto
       - Muestra informacion mas detallada por tipo:
           fijo     → 'Pago fijo: $500.00'
           porcentual → 'Porcentual: 10.0% ($500.00)'
           por_horas → 'Por horas: 8h × $15.00'
           por_tarea → 'Por tarea: $200.00'
           ganancias → '20.0% de ganancias'

     _hintComp() NUEVO:
       - Hint contextual en el BottomSheet de agregar colaborador segun tipo

     _resumenCard() AMPLIADO — nueva seccion PROYECCION:
       Titulo: "Proyeccion (si cobras todo el contrato)"
       Formula:
         totalColabAcordado = SUM(_calcMontoColaborador) de todos los teamMembers
         gananciaProyectada = montoTotal - totalColabAcordado - gastos_actuales
         margenProyectado   = gananciaProyectada / montoTotal * 100
       UI:
         - Fila: Ingresos esperados (monto_total del contrato)
         - Fila: Costos laborales acordados (SUM compensaciones)
         - Fila: Gastos operativos actuales (gastos reales ya registrados)
         - Fila: Ganancia proyectada (verde si >= 0, rojo si negativo) en bold
         - Barra LinearProgressIndicator con color segun margen:
             >= 30% → verde (success)
             >= 15% → amarillo (primary)
             < 15%  → rojo (danger)
         - Porcentaje de margen proyectado
         - Nota: "* Excluye colaboradores a % de ganancias" si aplica

     NOTA CRITICA SOBRE COLABORADORES CON "ganancias":
       Los colaboradores con tipo_compensacion='ganancias' se excluyen de la
       proyeccion porque su monto depende de la ganancia, lo que es circular.
       Se muestra un aviso al usuario si tiene este tipo de colaborador.

ENDPOINTS NUEVOS (total rutas /jobs ahora = 13):
  DELETE /jobs/:id/team-member-payments/:pid  ← NUEVO

VERIFICACION:
  - Crear trabajo con colaborador fijo $500
  - Badge muestra "Pendiente" (rojo), boton + visible
  - Tocar + → BottomSheet con monto pre-relleno $500.00
  - Confirmar → badge cambia a "Pagado" (verde), sub-tile muestra el pago
  - Tocar X en el sub-tile → pago eliminado, badge vuelve a "Pendiente"
  - Sin cobros del cliente → Resumen real muestra utilidad $0
  - Proyeccion muestra ganancia esperada = monto_total - $500 - gastos

================================================================================
21. SESION 2026-05-13 - SUBCATEGORIAS DE GASTOS + MODULO GUSTITOS
================================================================================

OBJETIVO:
  1. Agregar subcategorizacion a gastos existentes (fijo/variable/ahorro).
  2. Implementar el modulo Gustitos: compras espontaneas con conciencia financiera.

CAMBIOS BACKEND (server.js):

  1. POST /gastos — acepta campo opcional subcategoria (VARCHAR 100)
     La subcategoria se propaga al movimiento auto-creado (fijo/ahorro).

  2. POST /presupuestos/:id/movimientos — items[] ahora acepta subcategoria por item.
     Si no viene en el item, hereda del gasto origen.

  3. GET /presupuestos/:id/detalle — ACTUALIZADO:
     - Consulta totalGustitos: SUM(amount) FROM gustitos WHERE budget_id AND deleted_at IS NULL
     - balance_disponible = monto_total - totalGastado - totalGustitos
     - porcentaje_utilizado incluye Gustitos en el calculo
     - Respuesta incluye totalGustitos en el objeto resumen

  4. Nuevo modulo Gustitos (7 endpoints):
     POST   /gustitos
     GET    /gustitos?firebase_uid=
     GET    /gustitos/:id?firebase_uid=
     PATCH  /gustitos/:id
     DELETE /gustitos/:id?firebase_uid=  (soft delete)
     GET    /presupuestos/:budgetId/gustitos?firebase_uid=
     GET    /presupuestos/:budgetId/gustitos/summary?firebase_uid=

CAMBIOS BASE DE DATOS:
  Migración: database/migrations/add_gustitos_subcategorias.sql
  Ejecutada en Clever Cloud el 2026-05-13.
  - ALTER TABLE gastos ADD COLUMN subcategoria VARCHAR(100) NULL
  - ALTER TABLE movimientos ADD COLUMN subcategoria VARCHAR(100) NULL
  - CREATE TABLE gustitos (15 campos, soft delete, FK a presupuestos)

CAMBIOS FLUTTER:

  1. lib/widgets/presupuestos/gasto_form_sheet.dart
     - Nuevo selector de subcategoria: chips en Wrap (12 opciones predefinidas)
     - El chip seleccionado puede deseleccionarse tocandolo de nuevo
     - Callback onGuardado recibe subcategoria? opcional al final

  2. lib/services/gustitos_service.dart (NUEVO)
     - 5 metodos estaticos: listarPorPresupuesto, resumen, crear, eliminar, editar

  3. lib/gustitos/crear_gustito_sheet.dart (NUEVO)
     - Bottom sheet con: nombre, monto, comercio (opcional), fecha (date picker)
     - Chips de categoria (12) y emotion_tag (6: antojo/premio/social/impulso/estres/otro)
     - Mensaje guia: "Compra consciente sin culpa"

  4. lib/gustitos/gustitos_screen.dart (NUEVO)
     - Lista de Gustitos del presupuesto con total acumulado en header card
     - _GustitoTile: icono bolt, nombre, categoria, emocion, comercio, fecha, monto, eliminar
     - Soft delete con confirmacion AlertDialog
     - FAB "Agregar Gustito"
     - Estado vacio con mensaje amigable

  5. lib/detalles_presupuesto.dart (ACTUALIZADO)
     - Import: gustitos_service.dart, crear_gustito_sheet.dart, gustitos_screen.dart
     - Estado: double totalGustitos
     - _cargar(): lee resumen.totalGustitos
     - build(): totalConGustitos = totalGastado + totalGustitos para balance y barra
     - Stat card "Gustitos" aparece si totalGustitos > 0 (segunda fila de stat cards)
     - Nueva seccion GUSTITOS entre [botones de accion] y [MOVIMIENTOS]:
         Card con icono bolt, total, boton + (CrearGustitoSheet), boton → (GustitosScreen)
     - _MovimientoTile: muestra subcategoria como chip gris pequeño si existe
     - _agregarGasto() acepta subcategoria y la envia en POST /gastos y POST movimientos
     - _modalNuevoGasto() propaga subcategoria del GastoFormSheet

APK GENERADO:
  build/app/outputs/flutter-apk/app-release.apk  24.5 MB  (2026-05-13)
  Compilacion exitosa sin warnings relevantes.

REGLA DE EXPERIENCIA (GUSTITOS):
  NUNCA mostrar "gasto hormiga" en la UI. Siempre: "Gustito" / "Gustitos".
  Tono financiero amigable, sin generar culpa.
  Preparado para futura integracion QR: campo scanned_invoice_id nullable en tabla.

FLUJO FINANCIERO COMPLETO:
  balance_disponible = monto_total - SUM(movimientos.monto) - SUM(gustitos.amount)
  Los Gustitos no reemplazan gastos planificados, son una capa adicional de registro.

CONSIDERACIONES DE ESTABILIDAD:
  - Las columnas subcategoria son nullable: ninguna query existente se rompe.
  - La tabla gustitos es completamente nueva: ningun modulo existente la toca.
  - El calculo de balance en GET /detalle es aditivo: si no hay Gustitos, totalGustitos=0
    y el resultado es identico al anterior.
  - Soft delete en gustitos: al eliminar, el disponible se recupera en el proximo _cargar().

RIESGOS FUTUROS IDENTIFICADOS:
  - La integracion con QR (scanned_invoice_id) no esta implementada; solo el campo FK.
  - Los Gustitos no tienen periodo asociado: si un usuario tiene muchos Gustitos en
    presupuestos viejos, todos siguen descontando del disponible actual.
    Considerar filtrar por fecha en el futuro (ej: solo Gustitos del periodo activo).
  - La subcategoria es un VARCHAR libre: si en el futuro se necesita validacion estricta
    o catalogo de categorias, requiere migracion a FK hacia tabla categorias.

================================================================================
FIN DEL DOCUMENTO TECNICO - SALARYING
================================================================================
